<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    
    <style name="Nanodegree.Home.Button" parent="Widget.AppCompat.Button">
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:background">@color/normal_btn_bg</item>
        <item name="android:paddingLeft">14sp</item>
        <item name="android:paddingRight">14sp</item>
        <item name="android:layout_width">220sp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
